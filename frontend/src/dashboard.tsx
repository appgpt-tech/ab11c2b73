import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "tutoring platform" />
      <CardContent> A platform for managing students, activities and attendance. < /CardContent>
      </Card>
    );