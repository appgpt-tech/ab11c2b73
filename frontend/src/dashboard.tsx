import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "ShopSphere Backoffice" />
      <CardContent> My app is an ecommerce backoffice site to support a seamless shopping experience web app called ShopSphere for users worldwide < /CardContent>
      </Card>
    );