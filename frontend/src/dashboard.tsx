import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "tutoring management system" />
      <CardContent> An application for managing tutoring lessons, activities, students, their attendance and costs. < /CardContent>
      </Card>
    );