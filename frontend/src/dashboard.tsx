import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "ecommerceapplication" />
      <CardContent> E-commerce application with features like product listing, category filtering, transaction handling, and user management. < /CardContent>
      </Card>
    );