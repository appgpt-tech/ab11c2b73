import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "ecommerceapp" />
      <CardContent> An application managing an eCommerce store < /CardContent>
      </Card>
    );