import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "BookReviewApp" />
      <CardContent> An application for reviewing books < /CardContent>
      </Card>
    );